<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dgutforum.mapper.UserMapper" >

    <update id="update">
        update user
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="photo != null and photo != ''">
                photo = #{photo},
            </if>
            <if test="profile != null and profile != ''">
                profile = #{profile},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="email != null">
                email = #{email}
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="id_select" parameterType="java.util.List" resultType="com.dgutforum.user.pojo.FollowVo">
        select username,photo,id
        from user
        where id in
        <foreach collection="list" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </select>
</mapper>